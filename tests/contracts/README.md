# Upstream Maida contract snapshot

These files are exact test snapshots of the authoritative contract in
`maida/contracts/`. They drive local documentation assertions and are not an
independently owned website contract. Update Python first, then copy and verify
the snapshot with the workspace cross-repository sync checker.
